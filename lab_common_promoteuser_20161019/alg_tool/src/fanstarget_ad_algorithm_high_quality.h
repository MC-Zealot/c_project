#include <stdint.h>

//bool flowControl(uint64_t order_uid, uint32_t begin_time, int randomDecay, int source);
bool flowControl(uint64_t order_uid, uint32_t begin_time, uint32_t finish_amount, double theory_impression, int randomDecay, uint32_t source);
